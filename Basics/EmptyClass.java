/*
This program can only be compiled but can't be executed.
Why ?
The compiler is not dependent on main() but JVM depends on main(), hence the program will only be compiled but not executed.
*/

class EmptyClass{
	
	
}